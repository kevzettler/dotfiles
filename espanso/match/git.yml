# Automatically generated by espanso migration tool
# Original file: user/git.yml

matches:
  - trigger: gitb
    replace: git branch
  - trigger: gito
    replace: git checkout
  - trigger: gitc
    replace: "git commit -am \"$|$\""
  - trigger: gits
    replace: git status
  - trigger: gpum
    replace: git pull origin master
  - trigger: gpom
    replace: git push origin master
  - trigger: gpob
    replace: git push origin HEAD
  - trigger: gpof
    replace: git push -f origin HEAD
  - trigger: dckill
    replace: docker ps -a | tail -n +2 |  awk '{print $1}' | xargs docker kill; docker images | tail -n +2 |  awk '{print $3}' | xargs docker rmi; docker volume prune -f; docker system prune -f
  - trigger: sdckill
    replace: sudo docker ps -a | tail -n +2 |  awk '{print $1}' | xargs sudo docker kill; sudo docker images | tail -n +2 |  awk '{print $3}' | xargs sudo docker rmi; sudo docker system prune -f
