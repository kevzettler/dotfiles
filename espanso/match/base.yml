# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: jck
    replace: "ack $|$ --ignore-dir={build,dist,logs,coverage} ./"
  - trigger: grpi
    replace: grep -rni $|$ .
  - trigger: ":espanso"
    replace: Hi there!
  - trigger: npmi
    replace: npm install
  - trigger: ":date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%m/%d/%Y"
  - trigger: ":shell"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: echo Hello from your shell
  - trigger: "csdbl"
    replace: Debug.Log("$|$")
